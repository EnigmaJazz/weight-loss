# SDD Init Context — weight_loss

Generated by `sdd-init` on 2026-08-02. Canonical SDD config lives in `openspec/config.yaml`;
this file is the local cache for later SDD phases (Engram mem_* unavailable in this environment).

## Project

- Name: weight_loss (Weight Loss Tracker)
- Root: /home/james/weight_loss
- Git: repo on branch `main`, nothing committed yet (all files untracked)
- Purpose: single-user weight-loss tracker; no auth; Web Push notifications

## Stack (detected, not guessed)

- Python 3.14.6 (venv at `.venv/`)
- FastAPI 0.141.1 + uvicorn 0.52.1 (`uvicorn[standard]`, uvloop 0.22.1)
- SQLite via stdlib `sqlite3` (db `weight_loss.db`), single connection + lock
- Web Push: pywebpush 2.3.0 + py-vapid 1.9.4, keys in `vapid_keys.json`
- httpx 0.28.1 (ASGITransport tests), python-dotenv
- Frontend: vanilla JS SPA in `static/` (index.html, app.js, style.css, sw.js, manifest.webmanifest), no build step

## Architecture / Conventions

- Module-per-concern: `main.py` (create_app factory + lifespan + init_app_state),
  `routes.py` (/api/*), `database.py` (Database class + run_db thread helper),
  `rewards.py` (pure milestone logic), `notifications.py` (VAPID + send_push/send_to_all),
  `scheduler.py` (asyncio loop), `constants.py` (get_logger, defaults), `models.py` (dataclasses)
- State on `fastapi app.state`; no module-level mutable globals
- Async request path; sync sqlite calls wrapped with `await run_db(...)`
- Type hints required; dataclasses for structured state; no print() (use get_logger);
  no bare except
- Commits: conventional commits, NO Co-Authored-By / AI attribution
- `AGENTS.md` + `.gga` = pre-commit AI review (gga, RULES_FILE="AGENTS.md")

## Strict TDD

- **true** — no marker/config found; test runner exists (pytest) → default `strict_tdd: true`
  per sdd-init decision gates. Mirrored in `openspec/config.yaml` (`strict_tdd: true`,
  apply `tdd: true`).

## Persistence

- Mode: openspec (file-based). Artifacts under `openspec/`.
- Engram unavailable in this session → equivalent context cached in this file and
  `.atl/testing-capabilities.md`.

## Artifacts Created

- openspec/config.yaml
- openspec/specs/ (empty, .gitkeep)
- openspec/changes/ (empty, .gitkeep)
- openspec/changes/archive/ (empty, .gitkeep)
- .atl/init-context.md (this file)
- .atl/testing-capabilities.md
- .atl/skill-registry.md — existed already, current (2026-08-02), follows scan rules

## Next Steps

- `/sdd-explore` a change idea or `/sdd-new` to start a change.
