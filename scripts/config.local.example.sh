#!/usr/bin/env bash
# Local machine overrides for the backup/restore scripts.
#
# Copy this template to config.local.sh (gitignored) and set YOUR paths.
# When config.local.sh is absent the scripts fall back to $HOME-relative
# defaults, which are portable but may not match your setup.

# Directory containing this app (the git checkout).
# APP_DIR="$HOME/weight_loss"

# Where daily backups are stored. For real redundancy this should be a
# SEPARATE disk from the app (same-disk copies are not real backups).
# BACKUP_ROOT="$HOME/backups/weight-loss"
